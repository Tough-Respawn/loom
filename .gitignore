# Poids des modèles (trop lourds) — ignorés ; la config/profil par modèle (model.toml,
# profile.md sous loom/models/<id>/) reste suivie.
loom/models/*.gguf
loom/models/**/*.gguf
loom/data/
# Surcharges machine
*.local.toml
# Python / uv
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
*.pyc
# Node : aucune dépendance (libs front vendorées dans loom/web/static, zéro build)
node_modules/
# Traces de playwright-mcp (debug UI)
.playwright-mcp/
