# /rebench Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Commande chat `/rebench <id>` : recalibration topologique du contexte d'un modèle LOCAL TEXTE tel que configuré, verdict comparé, application sur « oui ».

**Architecture:** Wizard pur (`b_*` dans loom/web/wizard.py), effets dans routes.py (job thread + réutilisation de loom.setup : `topology.calibrate`, `ServerProbe`, `_set_model_context`, `read_raw_config`). Un seul job à la fois (état module).

**Tech Stack:** Python/Flask, loom.setup.topology, pytest. Spec : `docs/superpowers/specs/2026-07-19-rebench-design.md`.

## Global Constraints

- wizard.py PUR (aucun I/O) — tout passe par deps/action.
- Locaux texte uniquement ; mesure avec la config EXACTE du modèle (pas de sweep).
- Messages français, préfixe `[rebench …]` ; résultat final PERSISTÉ (journal).
- Suite `uv run pytest tests/ -q` + `ruff check` verts à chaque commit.

---

### Task 1: wizard — flux `b_*`

**Files:** Modify `loom/web/wizard.py` (+`_STEPS`), Test `tests/test_wizard_add_model.py`

**Interfaces:**
- Consumes: `deps.rebenchable_models() -> list[{"id","label"}]` ; `deps.model_kind(mid) -> "local"|"remote"|"image"|"video"|None`.
- Produces: `start_rebench(arg, deps) -> WizardResult` ; actions `{"kind":"rebench","id"}` et `{"kind":"rebench_apply","id","context","mecanisme"}` ; état `b_apply` posé PAR ROUTES à la fin du job : `{"step":"b_apply","id","context","mecanisme"}`.

- [ ] **Step 1: tests failing** (stub deps + `rebenchable_models=lambda: []`, `model_kind=lambda m: None` ajoutés au `deps()` du fichier)

```python
def test_rebench_liste_et_confirmation():
    items = [{"id": "orn", "label": "orn — contexte actuel 49152, 34.4 Go"}]
    d = deps()
    d.rebenchable_models = lambda: items
    r = wizard.start_rebench("", d)
    assert "orn" in r.reply and r.state["step"] == "b_pick"
    r = wizard.step(r.state, "1", d)
    assert r.state["step"] == "b_confirm" and "éteint" in r.reply
    r = wizard.step(r.state, "oui", d)
    assert r.state is None and r.action == {"kind": "rebench", "id": "orn"}

def test_rebench_id_direct_et_refus_par_type():
    items = [{"id": "orn", "label": "orn — contexte actuel 49152, 34.4 Go"}]
    d = deps()
    d.rebenchable_models = lambda: items
    d.model_kind = lambda m: {"glm": "remote", "chroma": "image"}.get(m)
    r = wizard.start_rebench("orn", d)
    assert r.state["step"] == "b_confirm"
    r = wizard.start_rebench("glm", d)
    assert r.state is None and "texte" in r.reply
    r = wizard.start_rebench("inconnu", d)
    assert r.state is None and "inconnu" in r.reply

def test_rebench_apply_oui_et_annulation():
    st = {"step": "b_apply", "id": "orn", "context": 65536, "mecanisme": "pente x"}
    r = wizard.step(st, "oui", deps())
    assert r.action == {"kind": "rebench_apply", "id": "orn", "context": 65536,
                        "mecanisme": "pente x"}
    r = wizard.step(st, "non", deps())
    assert r.state is None and r.action is None and "inchangé" in r.reply
```

- [ ] **Step 2:** `uv run pytest tests/test_wizard_add_model.py -q` → FAIL
- [ ] **Step 3: implémentation** (section `# ---------- flux /rebench ----------`)

```python
def start_rebench(arg: str, deps) -> WizardResult:
    items = deps.rebenchable_models()
    a = (arg or "").strip()
    if a:
        it = next((i for i in items if i["id"] == a), None)
        if it is None:
            kind = deps.model_kind(a)
            if kind in ("remote", "image", "video"):
                return WizardResult(None, (
                    f"« {a} » n'est pas calibrable : seuls les modèles LOCAUX "
                    "texte-à-texte se mesurent (image/vidéo = ComfyUI, distant = "
                    "la fenêtre du provider)."))
            return WizardResult(None, f"Modèle « {a} » inconnu. /rebench sans "
                                "argument liste les modèles calibrables.")
        return _rebench_confirm(it)
    if not items:
        return WizardResult(None, "Aucun modèle local texte à calibrer.")
    lines = [f"  {i + 1}. {it['label']}" for i, it in enumerate(items)]
    return WizardResult(
        {"step": "b_pick", "items": items},
        "[rebench 1/2] Quel modèle recalibrer ?\n" + "\n".join(lines)
        + "\n(réponds par un numéro — /cancel pour annuler)")

def _rebench_confirm(it) -> WizardResult:
    return WizardResult(
        {"step": "b_confirm", "id": it["id"]},
        f"[rebench 2/2] Recalibrer « {it['id']} » tel qu'il est configuré ? "
        "Durée ~5-20 min : le serveur modèle local sera éteint et les modèles "
        "locaux indisponibles pendant la mesure (les distants restent utilisables). "
        "La progression s'affiche ici.\nTape « oui » pour lancer — toute autre "
        "réponse annule.")

def _step_b_pick(state, t, deps):
    items = state["items"]
    if not (t.isdigit() and 1 <= int(t) <= len(items)):
        return WizardResult(state, "Réponds par le numéro du modèle (ou /cancel).")
    return _rebench_confirm(items[int(t) - 1])

def _step_b_confirm(state, t, deps):
    if t.lower() not in ("oui", "o", "yes"):
        return WizardResult(None, "Recalibration annulée.")
    return WizardResult(
        None, f"Recalibration de « {state['id']} » lancée…",
        {"kind": "rebench", "id": state["id"]})

def _step_b_apply(state, t, deps):
    if t.lower() not in ("oui", "o", "yes"):
        return WizardResult(None, "Config inchangée — rien n'a été touché.")
    return WizardResult(
        None, f"Application du contexte {state['context']} à « {state['id']} »…",
        {"kind": "rebench_apply", "id": state["id"], "context": state["context"],
         "mecanisme": state["mecanisme"]})
```

`_STEPS` += `{"b_pick": _step_b_pick, "b_confirm": _step_b_confirm, "b_apply": _step_b_apply}`.

- [ ] **Step 4:** suite verte → **Step 5:** commit `feat(wizard): flux /rebench (liste, confirmation, application)`

---

### Task 2: routes — interception, job de calibration, application

**Files:** Modify `loom/web/routes.py` (`_handle_add_model_command`, `_wizard_deps`, `CHAT_COMMANDS`, nouveaux helpers), Test `tests/test_rebench_routes.py` (créer)

**Interfaces:**
- Consumes: Task 1 ; `loom.setup.steps.read_raw_config/server_bin_status/resolve_bin` ; `loom.setup.topology` (`discover_topology`, `memory_budget_mb`, `ServerProbe`, `calibrate`, `gpu_vram_total_mb`) ; `loom.setup.bench.has_gpu_backend` ; `loom.setup.cli._set_model_context` ; `read_gguf_meta` ; `S.server_manager.stop()`.
- Produces: `_rebenchable_models(S)`, `_start_rebench_job(S, sess, chat_lock, mid)` (module-level `_REBENCH` = état du job unique), bras d'actions `rebench`/`rebench_apply`.

- [ ] **Step 1: tests failing** — fixture `env` réutilisée (pattern test_add_model_routes), monkeypatch de `routes._run_calibration` (cœur mesure isolé) :

```python
def test_rebench_refuse_sans_table_bench(env, monkeypatch): ...
    # raw_cfg sans [bench] -> le flux répond « loom-setup » et ne lance pas de job
def test_rebench_job_poste_verdict_et_etat_apply(env, monkeypatch): ...
    # _run_calibration stubbé -> {"context": 8192, ...} vs actuel 4096 :
    # dernier message persisté contient « oui pour appliquer », conv.wizard step b_apply
def test_rebench_deja_au_top(env, monkeypatch): ...
    # calibré == actuel -> message « déjà au top », PAS d'état wizard
def test_rebench_apply_ecrit_model_toml_et_registres(env, monkeypatch): ...
    # action apply -> model.toml contient context = 8192, S.model_contexts maj, yaml regen
def test_rebench_verrou_un_seul_job(env, monkeypatch): ...
    # job « en cours » simulé -> second /rebench répond « déjà en cours »
```

(Écrire ces tests EN ENTIER dans le fichier — asserts sur `_sse_texts`, le timeline.jsonl pour la persistance, et le model.toml du dossier modèle de test créé dans tmp avec `dir`/`context` dans `local_models` passés à `create_app`.)

- [ ] **Step 2:** FAIL → **Step 3: implémentation**

1. Interception : dans `_handle_add_model_command`, ajouter `is_rb = message == "/rebench" or message.startswith("/rebench ")` au même titre que `is_rm` ; dispatch `res = _wizard.start_rebench(message[len("/rebench"):].strip(), deps)`.
2. `_wizard_deps` += `rebenchable_models=lambda: _rebenchable_models(S)` et `model_kind=lambda mid: _model_kind(S, mid)`.

```python
def _model_kind(S, mid):
    if mid in S.remote_model_ids: return "remote"
    if mid in S.video_model_ids: return "video"
    if mid in S.image_model_ids: return "image"
    if any(m.get("id") == mid for m in S.local_model_specs): return "local"
    return None

def _rebenchable_models(S):
    return [{"id": m["id"],
             "label": f"{m['id']} — contexte actuel {m.get('context', '?')}, "
                      f"{(m.get('size_mb') or 0) / 1024:.1f} Go"}
            for m in S.local_model_specs]
```

3. Job unique (module-level) + cœur de mesure isolé (stub des tests) :

```python
_REBENCH = {"job": None}  # un seul rebench à la fois (machine saturée pendant la mesure)

def _run_calibration(S, spec, progress):
    """Cœur de mesure : préconditions + topologie + calibrate. Lève RuntimeError
    avec un message ACTIONNABLE si la machine n'est pas prête."""
    import os, psutil
    from loom.runtime.gguf_meta import read_gguf_meta
    from loom.setup import bench as bench_mod, topology as topo_mod
    from loom.setup.steps import read_raw_config, resolve_bin, server_bin_status
    from loom.web.__main__ import CONFIG_PATH, PERSONAL_CONFIG_PATH

    raw = read_raw_config(CONFIG_PATH, PERSONAL_CONFIG_PATH)
    tbl = raw.get("bench") or {}
    if not tbl.get("threads"):
        raise RuntimeError("machine pas encore calibrée — lance « uv run loom-setup » d'abord")
    _, bin_name = server_bin_status(raw)
    server_bin = resolve_bin(bin_name)
    if server_bin is None:
        raise RuntimeError("binaire llama-server introuvable")
    import tomllib
    mdir = Path(spec["dir"])
    mt = tomllib.loads((mdir / "model.toml").read_text(encoding="utf-8"))
    gguf = mdir / mt["filename"]
    meta = read_gguf_meta(gguf)
    is_moe = bool(meta.get("expert_count"))
    vram = topo_mod.gpu_vram_total_mb()
    topo = topo_mod.discover_topology(meta, bench_mod.has_gpu_backend(server_bin), vram)
    headroom = int((raw.get("server") or {}).get("gpu_kv_headroom_mb", 640) or 640)
    ram = int(psutil.virtual_memory().total // (1024 * 1024))
    budget = topo_mod.memory_budget_mb(topo, vram, ram, headroom)
    mmproj = mt.get("mmproj_filename")
    probe = topo_mod.ServerProbe(
        server_bin=str(server_bin), model_path=str(gguf), threads=tbl["threads"],
        ngl=99 if (is_moe and topo != topo_mod.TOPO_RAM) else tbl.get("ngl", 0),
        topology=topo, mmproj_path=str(mdir / mmproj) if mmproj else None,
        cpu_moe=bool(mt.get("cpu_moe", is_moe)), n_cpu_moe=mt.get("n_cpu_moe"))
    progress(f"topologie {topo}, budget {budget} Mo")
    calib = topo_mod.calibrate(probe, meta, topology=topo, budget_mb=budget,
                               progress=progress)
    return calib, gguf
```

4. Bras `rebench` dans le handler : verrou (`_REBENCH["job"]` non fini → `extra_reply` « déjà en cours »), sinon `S.server_manager.stop()`, création d'un objet job (`SimpleNamespace(done=False, label="", final=None)`), thread daemon :

```python
def _rebench_worker(S, sess, chat_lock, mid, job):
    spec = next((m for m in S.local_model_specs if m.get("id") == mid), None)
    try:
        calib, gguf = _run_calibration(
            S, spec, lambda m: setattr(job, "label", f"calibration : {m}"))
        current = int(spec.get("context") or S.context_window or 0)
        new = calib["context"]
        if new == current:
            msg = (f"✅ « {mid} » est déjà au top : contexte actuel {current} = "
                   f"mesuré {new} ({calib['mecanisme']}). Rien à changer.")
            wiz = None
        else:
            sens = "amélioration" if new > current else "RÉDUCTION (l'actuel déborde)"
            msg = (f"Verdict pour « {mid} » : {sens} — contexte {current} → {new} "
                   f"(pente {calib['slope_kb_tok']} Ko/token, vitesse validée "
                   f"jusqu'à {calib['valide_jusqua']} tokens).\n"
                   f"mécanisme : {calib['mecanisme']}\n"
                   "Tape « oui » pour appliquer — toute autre réponse laisse tout en l'état.")
            wiz = {"step": "b_apply", "id": mid, "context": new,
                   "mecanisme": calib["mecanisme"], "gguf": str(gguf)}
    except (RuntimeError, ValueError) as exc:
        msg, wiz = f"❌ Recalibration de « {mid} » échouée : {exc} — config inchangée.", None
    job.done = True
    got = chat_lock.acquire(timeout=2)
    try:
        conv = sess.conversation
        conv.add("assistant", msg)
        if wiz is not None:
            conv.set_wizard(wiz)
        S.session_store.append_event(sess.id, "text", {"text": msg})
        S.session_store.save(sess)
        job.final = msg
    finally:
        if got: chat_lock.release()
```

5. Stream : dans `_stream()`, branche dédiée (le job download garde la sienne) :

```python
        if rb_job is not None:
            while not rb_job.done:
                yield _sse("status", label=rb_job.label or "calibration en cours…")
                time.sleep(2)
            yield _sse("status", label="")
            if rb_job.final:
                yield _sse("text", text="\n" + rb_job.final)
```

6. Bras `rebench_apply` : `from loom.setup.cli import _set_model_context` ; le state `b_apply` porte `gguf` → wizard le repasse ? NON (wizard pur renvoie id/context/mecanisme seulement) → retrouver le gguf depuis `S.local_model_specs[id]["dir"]` + model.toml filename ; `_set_model_context(gguf, context, mecanisme)` ; `spec["context"] = context` ; `S.model_contexts[mid] = context` ; `_regen_swap_yaml(S)` ; `extra_reply = "✅ contexte {context} écrit dans model.toml — effet au prochain chargement du modèle."`.
7. `CHAT_COMMANDS` += entrée `/rebench` (« Recalibrer un modèle local texte (contexte/vitesse mesurés, tel que configuré) — verdict puis application sur confirmation. »).

- [ ] **Step 4:** suite + ruff verts → **Step 5:** commit `feat(web): /rebench — recalibration d'un local texte depuis le chat`

---

### Task 3: E2E réel

- [ ] Instance éphémère port 8001 (`run_loom_e2e.py` du scratchpad), session de test.
- [ ] `/rebench` → liste = locaux texte seulement ; `/rebench glm-zai` → refus « texte ».
- [ ] `/rebench gemma4-e4b-heretic` → « oui » → progression visible → verdict réel.
- [ ] Si verdict propose : « oui » → model.toml de gemma4 vérifié sur disque (context + note) ; sinon vérifier le message « déjà au top ».
- [ ] Recharger la page → verdict persisté. Nettoyage session + instance. Rapport.
